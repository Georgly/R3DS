#ifndef TEXTURECOORD_H
#define TEXTURECOORD_H


class TextureCoord
{
public:
    float u, v, w;
    TextureCoord();
    TextureCoord(float u, float v, float w);
};

#endif // TEXTURECOORD_H
